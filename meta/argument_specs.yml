# This is an example of argument validation for the role
---
argument_specs:
  main:
    short_description: validation for params in tasks/main.yml
    options:
      ROLENAME_task_prefix:
        type: "string"
        required: true
        default: "=== ROLENAME ==="
        description: "Task name prefix"
...
